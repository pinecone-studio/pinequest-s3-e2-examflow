mutation CreateQuestionVariantsMutation(
  $sourceQuestionId: ID!
  $totalVariants: Int!
) {
  createQuestionVariants(
    sourceQuestionId: $sourceQuestionId
    totalVariants: $totalVariants
  ) {
    id
  }
}
